# Enable build cache
org.gradle.caching=true
# Use parallel execution
org.gradle.parallel=true
# Configuration cache causes issues with PKL plugin serialization, so disable it
# org.gradle.configuration-cache=true